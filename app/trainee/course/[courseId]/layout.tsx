export default function CoursePlayerLayout({ children }: { children: React.ReactNode }) {
  // Focus Mode layout: absolutely blank to let the CoursePlayer claim 100% of the screen.
  return <>{children}</>;
}
